{
  "name": "emdash-remover",
  "version": "1.0.0",
  "description": "Chrome extension that appends NO EMDASHES to LLM messages",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --watch",
    "clean": "rm -rf dist",
    "package": "bun run build && cd dist && zip -r ../emdash-remover.zip . && cd .. && echo '✓ Package created: emdash-remover.zip'"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.27",
    "bun-types": "^1.3.1"
  }
}
